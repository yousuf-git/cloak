# Bundled builds talk to the sidecar backend that the app spawns on launch.
# Must match sidecar::API_PORT in src-tauri/src/sidecar/mod.rs.
# No secrets here — checked in on purpose so `pnpm ship` is reproducible.
VITE_API_URL=http://127.0.0.1:47821/api/v1
